# a simple example location scraper written in Go

## has an array of lat/longs it iterates over and fires off requests to the locator app for Cookout

### cookout locations for those lat-longs go to stdout